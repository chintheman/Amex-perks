# Hero image

The hero renders `assets/amex-platinum-card.jpg` when it exists and falls back
to a dashed placeholder when it does not — the page is fully functional either
way. Drop a photo of the card here at roughly 3:2 (the frame is 310 × 195 CSS
pixels, so 930 × 586 or larger keeps it sharp on retina screens).
